export class Brush {
    public fillColor: string;
    public lineWidth: number;
    constructor(fillColor: string, lineWidth: number) {
        this.fillColor = fillColor;
        this.lineWidth = lineWidth;
    }
}